name: Replay alert translations in TEST

on:
  push:
    branches: [fix/ground-alert-translations]

permissions:
  contents: read

env:
  RAILWAY_PROJECT_ID: 09fb396b-d91b-41b8-a70b-dd033d408e7d
  RAILWAY_STAGING_ENVIRONMENT_ID: 1f687c32-c70a-4d83-a19a-319f197ce772
  RAILWAY_SERVICE_ID: ed89feb6-f6a9-4cf4-8dbb-159de7439349

jobs:
  replay:
    runs-on: ubuntu-latest
    env:
      RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: pip
      - run: python -m pip install -r requirements.txt
      - run: python -m py_compile monitor.py replay_alert_translations.py
      - run: python -m unittest discover -s tests -v
      - run: npm install --global @railway/cli
      - name: Replay against staging secrets and TEST destinations
        run: >-
          railway run
          --project "$RAILWAY_PROJECT_ID"
          --environment "$RAILWAY_STAGING_ENVIRONMENT_ID"
          --service "$RAILWAY_SERVICE_ID"
          python replay_alert_translations.py
